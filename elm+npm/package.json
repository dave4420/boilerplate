{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "dev": "scripts/dev.sh",
    "dev:cat": "mkdir -p dist-cat && elm-live --port=8001 --dir=./dist-cat --pushstate -- src/Catalogue.elm --output=dist-cat/index.html",
    "elm": "elm",
    "format": "scripts/format.sh",
    "test": "scripts/test.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.7",
    "elm-live": "^4.0.2",
    "nodemon": "^3.1.0"
  }
}
