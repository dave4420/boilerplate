{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "dev": "mkdir -p dist && elm-live --dir=./dist -- src/Main.elm --output=dist/elm-dev.js --debug",
    "dev:cat": "mkdir -p dist-cat && elm-live --port=8001 --dir=./dist-cat -- src/Catalogue.elm --output=dist-cat/elm.js",
    "elm": "elm",
    "format": "scripts/format.sh",
    "test": "scripts/test.sh"
  },
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.7",
    "elm-live": "^4.0.2"
  }
}
